# 구현 로드맵

**작성일**: 2025-11-29  
**목표**: 마이페이지 백엔드 API 완전 연동

---

## 📅 **단계별 구현 계획**

---

## 🎯 **Phase 1: 기반 작업 (선행 필수)**

### ✅ Task 1.1: 타입 불일치 해결
**우선순위**: 🔴 최우선  
**예상 시간**: 30분

**작업 내용:**
1. `src/types/index.ts` 수정
   - `accountApproved` → `approvalStatus`
2. `src/store/authStore.ts` Mock 데이터 수정
3. `src/pages/MyPage.tsx` 필드명 수정
4. 전체 프로젝트 검색 및 일괄 수정
5. 빌드 에러 확인

**산출물:**
- [x] 타입 정의 파일 수정
- [x] authStore Mock 데이터 수정
- [x] MyPage 렌더링 코드 수정

**검증:**
```bash
# 빌드 에러 확인
npm run build

# approvalStatus 검색 (모두 통일되었는지)
grep -r "accountApproved" src/
```

---

### ✅ Task 1.2: API Service 파일 생성
**우선순위**: 🔴 최우선  
**예상 시간**: 1시간

**파일 생성:** `src/services/mypageService.ts`

**구현 함수:**
```typescript
// 1. 프로필 조회
export async function getProfile(): Promise<Account>

// 2. 프로필 수정
export async function updateProfile(data: UpdateProfileRequest): Promise<Account>

// 3. 계정 삭제
export async function deleteAccount(): Promise<void>
```

**산출물:**
- [x] mypageService.ts 파일
- [x] API 함수 3개 구현
- [x] 타입 정의 (UpdateProfileRequest)
- [x] Axios 에러 처리

**참고:**
- `src/services/api/client.ts` (기존 Axios 인스턴스 사용)
- `docs/api/03_mypage.md` (백엔드 API 명세)

---

## 🛠️ **Phase 2: 프로필 조회 기능**

### ✅ Task 2.1: 프로필 조회 API 연동
**우선순위**: 🟠 높음  
**예상 시간**: 1시간

**작업 내용:**
1. MyPage.tsx에 useQuery 추가
2. 페이지 마운트 시 프로필 조회
3. authStore 데이터와 병합/업데이트
4. 로딩/에러 상태 처리

**구현 위치:** `src/pages/MyPage.tsx`

```typescript
const { data: profile, isLoading, isError } = useQuery({
    queryKey: ['mypage', 'profile'],
    queryFn: getProfile,
    enabled: isAuthenticated,
});

useEffect(() => {
    if (profile) {
        setUser(profile); // authStore 업데이트
    }
}, [profile]);
```

**산출물:**
- [x] useQuery 훅 추가
- [x] 로딩 스켈레톤 UI
- [x] 에러 메시지 표시
- [x] authStore 동기화 로직

**검증:**
```bash
# 백엔드 서버 실행 확인
curl -H "Authorization: Bearer {token}" http://localhost:8081/api/mypage/profile
```

---

## ✏️ **Phase 3: 프로필 수정 기능**

### ✅ Task 3.1: 프로필 수정 모달 생성
**우선순위**: 🟠 높음  
**예상 시간**: 2시간

**파일 생성:** `src/components/mypage/EditProfileModal.tsx`

**구현 기능:**
1. 모달 UI (열기/닫기)
2. 폼 입력 필드
   - userName (2~50자)
   - phoneNumber (010-XXXX-XXXX)
3. 실시간 Validation
4. 에러 메시지 표시
5. 제출 처리

**기술 스택:**
- React Hook Form (폼 관리)
- utils/validation.ts (Validation 함수)
- TanStack Query useMutation (API 호출)

**산출물:**
- [x] EditProfileModal.tsx 컴포넌트
- [x] 폼 Validation 로직
- [x] 에러 처리 (409, 400)
- [x] 성공 시 authStore 업데이트

**UI 요구사항:**
```
┌─────────────────────────────────────┐
│ 프로필 수정                  [X]    │
├─────────────────────────────────────┤
│ 사용자 이름 *                       │
│ ┌─────────────────────────────────┐ │
│ │ 홍길동                          │ │
│ └─────────────────────────────────┘ │
│ ⚠️ 2~50자 입력해주세요              │
│                                     │
│ 전화번호 *                          │
│ ┌─────────────────────────────────┐ │
│ │ 010-1234-5678                   │ │
│ └─────────────────────────────────┘ │
│ ✅ 사용 가능한 번호입니다            │
│                                     │
│ [취소]              [저장]          │
└─────────────────────────────────────┘
```

---

### ✅ Task 3.2: MyPage에 수정 모달 통합
**우선순위**: 🟠 높음  
**예상 시간**: 30분

**작업 내용:**
1. "프로필 수정" 버튼 클릭 이벤트
2. 모달 열기/닫기 상태 관리
3. 수정 성공 시 프로필 재조회

**구현 위치:** `src/pages/MyPage.tsx`

```typescript
const [isEditModalOpen, setIsEditModalOpen] = useState(false);

<button 
    onClick={() => setIsEditModalOpen(true)}
    className="w-full btn-secondary flex items-center justify-center gap-2"
>
    <Edit className="w-4 h-4" />
    프로필 수정
</button>

<EditProfileModal
    isOpen={isEditModalOpen}
    onClose={() => setIsEditModalOpen(false)}
    user={user}
    onSuccess={(updatedUser) => {
        setUser(updatedUser);
        setIsEditModalOpen(false);
    }}
/>
```

**산출물:**
- [x] 모달 상태 관리
- [x] 버튼 클릭 이벤트
- [x] 모달 컴포넌트 렌더링

---

## 🗑️ **Phase 4: 계정 삭제 기능**

### ✅ Task 4.1: 계정 삭제 모달 생성
**우선순위**: 🟡 중간  
**예상 시간**: 1.5시간

**파일 생성:** `src/components/mypage/DeleteAccountModal.tsx`

**구현 기능:**
1. 경고 메시지 표시
2. 확인 텍스트 입력 ("계정 삭제" 정확히 입력)
3. 삭제 확인 버튼
4. 삭제 API 호출
5. 성공 시 로그아웃 + 리다이렉트

**산출물:**
- [x] DeleteAccountModal.tsx 컴포넌트
- [x] 확인 텍스트 검증
- [x] 삭제 API 호출
- [x] 로그아웃 처리

**UI 요구사항:**
```
┌─────────────────────────────────────┐
│ ⚠️ 계정 삭제                 [X]    │
├─────────────────────────────────────┤
│ 정말로 계정을 삭제하시겠습니까?      │
│                                     │
│ ⚠️ 주의사항:                        │
│ • 삭제된 계정은 복구할 수 없습니다   │
│ • 작성한 글/댓글은 유지됩니다        │
│ • 이메일/전화번호는 재사용 가능합니다 │
│                                     │
│ 계속하려면 "계정 삭제"를 입력하세요: │
│ ┌─────────────────────────────────┐ │
│ │                                 │ │
│ └─────────────────────────────────┘ │
│                                     │
│ [취소]            [삭제하기]        │
└─────────────────────────────────────┘
```

---

### ✅ Task 4.2: MyPage에 삭제 버튼 추가
**우선순위**: 🟡 중간  
**예상 시간**: 30분

**작업 내용:**
1. "계정 삭제" 버튼 추가 (사이드바 하단)
2. 모달 열기/닫기 상태 관리
3. 삭제 성공 시 로그아웃 처리

**구현 위치:** `src/pages/MyPage.tsx`

```typescript
const [isDeleteModalOpen, setIsDeleteModalOpen] = useState(false);

// 사이드바 마지막에 추가
<button 
    onClick={() => setIsDeleteModalOpen(true)}
    className="w-full btn-danger flex items-center justify-center gap-2 mt-4"
>
    <AlertTriangle className="w-4 h-4" />
    계정 삭제
</button>

<DeleteAccountModal
    isOpen={isDeleteModalOpen}
    onClose={() => setIsDeleteModalOpen(false)}
    onSuccess={() => {
        logout();
        navigate('/login');
    }}
/>
```

**산출물:**
- [x] 삭제 버튼 UI
- [x] 모달 상태 관리
- [x] 로그아웃 처리

---

## 🧪 **Phase 5: 테스트 및 검증**

### ✅ Task 5.1: 단위 테스트
**우선순위**: 🟢 낮음  
**예상 시간**: 2시간

**테스트 항목:**
1. mypageService.ts 함수 테스트
2. EditProfileModal 렌더링 테스트
3. DeleteAccountModal 렌더링 테스트
4. Validation 로직 테스트

**도구:**
- Vitest
- React Testing Library

---

### ✅ Task 5.2: 통합 테스트
**우선순위**: 🟡 중간  
**예상 시간**: 2시간

**시나리오:**
1. 프로필 조회 성공
2. 프로필 수정 성공
3. 전화번호 중복 시 409 에러
4. Validation 실패 시 400 에러
5. 계정 삭제 성공
6. 삭제된 계정 로그인 차단 (401)

---

## 📊 **진행 현황 추적**

| Phase | Task | 상태 | 예상 시간 | 실제 시간 |
|-------|------|------|-----------|-----------|
| 1 | 타입 불일치 해결 | ⬜ 대기 | 30분 | - |
| 1 | API Service 생성 | ⬜ 대기 | 1시간 | - |
| 2 | 프로필 조회 연동 | ⬜ 대기 | 1시간 | - |
| 3 | 수정 모달 생성 | ⬜ 대기 | 2시간 | - |
| 3 | 수정 모달 통합 | ⬜ 대기 | 30분 | - |
| 4 | 삭제 모달 생성 | ⬜ 대기 | 1.5시간 | - |
| 4 | 삭제 버튼 추가 | ⬜ 대기 | 30분 | - |
| 5 | 단위 테스트 | ⬜ 대기 | 2시간 | - |
| 5 | 통합 테스트 | ⬜ 대기 | 2시간 | - |

**총 예상 시간**: 11시간

---

## 🚫 **제외 사항 (백엔드 API 없음)**

아래 기능은 백엔드 API 개발 후 별도 구현:

- [ ] 작성한 글 목록 (GET /api/mypage/posts)
- [ ] 작성한 댓글 목록 (GET /api/mypage/comments)
- [ ] 찜한 과정 목록 (GET /api/mypage/bookmarks)
- [ ] 통계 데이터 (GET /api/mypage/statistics)
- [ ] 최근 활동 로그 (GET /api/mypage/activities)

---

## 🔗 **관련 문서**

- 백엔드 API 명세: `Java/docs/api/03_mypage.md`
- 프론트 가이드: `softwarecampus-frontend/docs/account/03_mypage_frontend.md`
- Mock 데이터 현황: `01_Mock_데이터_현황.md`
- 타입 불일치 문제: `02_타입_불일치_문제.md`
