# 커뮤니티 연동 작업 체크리스트

> **📅 업데이트:** 2024-12-01 (백엔드 검증 후 수정)
> **📍 상세 검증 결과:** [04_백엔드_API_검증_결과.md](./04_백엔드_API_검증_결과.md)

## Phase 0: API 파라미터/타입 수정 (최우선) 🔴 NEW

### 0.1 communityService.ts 파라미터 수정
- [ ] `fetchBoardPosts()` 파라미터명 변경
  - `page` → `pageNo`
  - `keyword` → `searchText`
  - `limit` 파라미터 제거 (백엔드 미지원)
- [ ] `createBoardPost()` FormData 방식으로 변경
  - JSON Body → `multipart/form-data`
  - `isSecret` → `secret`
  - 파일 업로드 지원 추가

### 0.2 types.ts 필드명 수정
- [ ] `ApiBoardListResponse.commentCount` → `commentsCount`

### 0.3 페이지네이션 응답 처리 수정
- [ ] Spring Page 응답 타입 정의 추가
- [ ] `fetchBoardPosts()` 응답에서 `totalElements` 사용

---

## Phase 1: 인증 연동 (우선)

### 1.1 Mock User 제거 및 실제 인증 연동
- [ ] `CommunityDetailPage.tsx` - Mock user 제거
- [ ] `CommunityWritePage.tsx` - Mock user 제거
- [ ] `CommunityEditPage.tsx` - Mock user 제거 (확인 필요)
- [ ] `useAuthStore`에서 현재 로그인 사용자 정보 사용

```typescript
// Before
const user = { id: 1, userName: '현재사용자' };

// After
import { useAuthStore } from '../store/authStore';
const { user, isAuthenticated } = useAuthStore();
```

### 1.2 비로그인 사용자 처리
- [ ] 글쓰기 버튼 클릭 시 로그인 페이지로 리다이렉트
- [ ] 댓글 작성 시 로그인 필요 안내
- [ ] 추천 버튼 클릭 시 로그인 필요 안내

---

## Phase 2: 누락 API 구현

### 2.1 communityService.ts 추가 함수 (백엔드 검증 완료)

```typescript
// 게시글 수정 (FormData 필수)
export const updateBoardPost = async (
  postId: number, 
  data: { title?: string; text?: string; secret?: boolean },
  files?: File[]
): Promise<void> => {
  const formData = new FormData();
  formData.append('id', String(postId));  // id 필수!
  if (data.title) formData.append('title', data.title);
  if (data.text) formData.append('text', data.text);
  formData.append('secret', String(data.secret ?? false));
  if (files) {
    files.forEach(file => formData.append('files', file));
  }
  await apiClient.patch(`/api/boards/${postId}`, formData);
};

// 게시글 삭제
export const deleteBoardPost = async (postId: number): Promise<void> => {
  await apiClient.delete(`/api/boards/${postId}`);
};

// 추천 취소 (새로 발견)
export const cancelRecommendBoardPost = async (postId: number): Promise<void> => {
  await apiClient.delete(`/api/boards/${postId}/recommends`);
};

// 첨부파일 다운로드 (새로 발견)
export const downloadBoardAttachment = async (
  boardId: number, 
  attachId: number
): Promise<Blob> => {
  const response = await apiClient.get(
    `/api/boards/${boardId}/boardAttachs/${attachId}/download`,
    { responseType: 'blob' }
  );
  return response.data;
};

// 댓글 추천 (새로 발견)
export const recommendComment = async (boardId: number, commentId: number): Promise<void> => {
  await apiClient.post(`/api/boards/${boardId}/comments/${commentId}/recommends`);
};

// 댓글 추천 취소 (새로 발견)
export const cancelRecommendComment = async (boardId: number, commentId: number): Promise<void> => {
  await apiClient.delete(`/api/boards/${boardId}/comments/${commentId}/recommends`);
};
```

### 2.2 구현 체크리스트
- [ ] `updateBoardPost` 함수 추가 (FormData 방식)
- [ ] `deleteBoardPost` 함수 추가
- [ ] `cancelRecommendBoardPost` 함수 추가
- [ ] `downloadBoardAttachment` 함수 추가
- [ ] `recommendComment`, `cancelRecommendComment` 함수 추가
- [ ] `CommunityEditPage.tsx`에서 `updateBoardPost` 사용
- [ ] `CommunityDetailPage.tsx`에 삭제 버튼 및 기능 추가

---

## Phase 3: 목록 조회 개선 (백엔드 검증 완료 ✅)

### 3.1 Spring Page 응답 처리

```typescript
// Spring Page 타입 정의
interface SpringPage<T> {
  content: T[];
  totalElements: number;
  totalPages: number;
  size: number;
  number: number;  // 0-indexed 주의!
  first: boolean;
  last: boolean;
  empty: boolean;
}

// fetchBoardPosts 수정
const response = await apiClient.get<SpringPage<ApiBoardListResponse>>('/api/boards', { params });
return { 
  posts: response.data.content.map(dto => ({ ... })),
  total: response.data.totalElements,
  totalPages: response.data.totalPages,
  currentPage: response.data.number + 1  // 0-indexed → 1-indexed
};
```

### 3.2 체크리스트
- [x] 백엔드 목록 응답 구조 확인 → **Spring Page 객체 사용**
- [ ] `fetchBoardPosts` 반환 타입 수정
- [ ] `CommunityPage.tsx` 페이징 로직 수정 (0-indexed 주의)
- [ ] 조회수(`hits`), 추천수(`likeCount`) → **목록에서 미제공 (상세에서만)**

---

## Phase 4: 정렬 및 검색 기능

### 4.1 정렬 기능
- [x] 백엔드 `sortType` 파라미터 지원 확인 → **❌ 미지원**
- [ ] 백엔드에 정렬 파라미터 추가 요청 또는
- [ ] 프론트엔드에서 정렬 UI 제거/비활성화

### 4.2 검색 기능
- [x] 백엔드 `searchType`, `searchText` 파라미터 지원 확인 → **✅ 지원**
- [ ] 검색어 파라미터명 수정 (`keyword` → `searchText`)

---

## Phase 5: 파일 첨부 기능 (백엔드 검증 완료 ✅)

### 5.1 파일 업로드 (게시글 작성/수정과 통합)
- [x] 파일 업로드 API 확인 → **POST/PATCH /api/boards (MultipartFile[])**
- [ ] `CommunityWritePage.tsx`에 파일 첨부 UI 추가
- [ ] `createBoardPost` FormData 방식으로 수정
- [ ] 파일 미리보기 기능
- [ ] 파일 크기/형식 검증

### 5.2 파일 다운로드
- [x] 다운로드 API 확인 → **GET /api/boards/{id}/boardAttachs/{attachId}/download**
- [ ] `CommunityDetailPage.tsx`에 첨부파일 목록 표시
- [ ] `downloadBoardAttachment` 함수 구현
- [ ] 파일 다운로드 버튼 연결

---

## Phase 6: UX 개선

### 6.1 에러 처리
- [ ] API 에러 시 사용자 친화적 메시지 표시
- [ ] 네트워크 오류 시 재시도 버튼
- [ ] 권한 오류 시 적절한 안내

### 6.2 로딩 상태
- [ ] 스켈레톤 UI 적용 (목록, 상세)
- [ ] 버튼 로딩 상태 표시

### 6.3 기타
- [ ] 삭제 확인 모달
- [ ] 추천 성공 피드백 (토스트 메시지)
- [ ] 댓글 작성 성공 피드백

---

## 진행 상태 요약

| Phase | 항목 | 상태 |
|-------|------|------|
| 0 | API 파라미터/타입 수정 | 🔴 **최우선** |
| 1 | 인증 연동 | ⏳ 대기 |
| 2 | 누락 API 구현 | ⏳ 대기 |
| 3 | 목록 조회 개선 | ⏳ 대기 |
| 4 | 정렬/검색 기능 | 🟡 정렬 미지원 |
| 5 | 파일 첨부 기능 | ⏳ 대기 |
| 6 | UX 개선 | ⏳ 대기 |

---

## 백엔드 확인 결과 (2024-12-01)

1. **게시글 수정/삭제 API** → ✅ **존재함**
   - `PATCH /api/boards/{id}` (FormData, 권한 체크)
   - `DELETE /api/boards/{id}` (권한 체크)

2. **목록 조회 응답 구조** → ✅ **Spring Page 사용**
   - `totalElements`, `totalPages` 제공
   - `hits`, `likeCount`는 목록에서 미제공 (상세에서만)

3. **정렬 파라미터** → ❌ **미지원**
   - 백엔드에 추가 요청 필요

4. **파일 첨부 API** → ✅ **통합됨**
   - 업로드: `POST/PATCH /api/boards` (MultipartFile[] files)
   - 다운로드: `GET /api/boards/{id}/boardAttachs/{attachId}/download`

5. **추가 발견 API** → 🆕
   - 게시글 추천 취소: `DELETE /api/boards/{id}/recommends`
   - 댓글 추천: `POST /api/boards/{boardId}/comments/{commentId}/recommends`
   - 댓글 추천 취소: `DELETE /api/boards/{boardId}/comments/{commentId}/recommends`
   - 대댓글 지원: `topCommentId` 파라미터
